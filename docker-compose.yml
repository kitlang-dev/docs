services:
  mkdocs:
    image: squidfunk/mkdocs-material
    container_name: mkdocs
    networks:
      - internal-net
    ports:
      - "1029:8000"
    volumes:
      - .:/docs
    command: serve --dev-addr=0.0.0.0:8000

networks:
  internal-net:
    external: true
